<%
  page_title = Typus::I18n.t("新規作成: %{resource}", :resource => @resource.model_name.human)
  title(page_title)
%>

<% content_for :main_grid do %>
  <h2>
    <%= page_title %>
    <small><%= list_actions %></small>
  </h2>

  <%= display_flash_message %>
  <%= render 'new' %>

  <% locals = { :options => params.dup.cleanup.merge(:action => 'create') } %>
  <%= render 'form', locals %>
<% end %>

<div class="grid_2">
  <%= build_sidebar %>
</div>

<div class="grid_8">
  <%= yield :main_grid %>
</div>
